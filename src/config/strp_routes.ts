import axios from 'axios';
import { BASE_PAY_URL } from './bots';

export const createStripeCustomer = async (email: string) => {
    try {
        
    } catch (err: any) {
        return console.log({ error: err.message });
    }
};
